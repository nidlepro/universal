{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "dependencies": {
    "conventional-changelog": "^3.1.21",
    "lerna": "^3.22.0",
    "@arekzc/chronos-cli": "*"
  }
}
