{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish-all": "chronos publish"
  },
  "dependencies": {
    "@arekzc/chronos-cli": "*",
    "@yarnpkg/pnpify": "^2.0.0-rc.24",
    "conventional-changelog": "^3.1.21",
    "lerna": "^3.22.0"
  },
  "version": "0.0.0",
  "devDependencies": {
    "eslint": "^7.2.0",
    "prettier": "^2.0.5",
    "stylelint": "^13.6.0",
    "typescript": "^3.9.5"
  }
}
