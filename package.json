{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "publish": "chronos publish"
  },
  "dependencies": {
    "conventional-changelog": "^3.1.21",
    "lerna": "^3.22.0",
    "@arekzc/chronos-cli": "*"
  }
}
