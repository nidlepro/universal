import path from 'path'

export const tsConfig = path.join(process.cwd(), 'tsconfig.json')

export const eslintrc = path.join(process.cwd(), '.eslintrc.js')

export const stylelintrc = path.join(process.cwd(), 'stylelint.config.js')
